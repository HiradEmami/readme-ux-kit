[project]
name = "readme-ux-kit-tools"
version = "0.1.0"
description = "Local tooling, generators, and studio app for readme-ux-kit."
readme = "README.md"
requires-python = ">=3.12.0"
dependencies = [
    "fastapi>=0.116.0",
    "pillow>=11.0.0",
    "playwright>=1.46.0",
    "tzdata>=2026.3",
    "uvicorn[standard]>=0.35.0",
]
